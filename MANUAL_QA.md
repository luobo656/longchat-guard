# V1 真实网页手工验收

在 Chrome / Edge 真实 `chatgpt.com` 页面执行。自动化测试通过不替代本清单。

## 0. 验收前命令

```text
npm run typecheck
npm test
npm run build
```

加载扩展目录：

```text
F:\ChatGPT 长会话提前预警器\dist
```

## 1. 基础加载

- [ ] Chrome 可加载 `dist`
- [ ] Edge 可加载同一 `dist`
- [ ] 打开 `https://chatgpt.com/` 后右下角出现状态胶囊
- [ ] 不需要 OpenAI API Key
- [ ] 不需要插件账号
- [ ] 不自动弹浏览器通知
- [ ] 扩展图标显示为深青绿底板、白色气泡、橙色守护盾牌，在 16px/32px 下仍能辨认，且不是 ChatGPT/OpenAI logo
- [ ] manifest 名称、描述和 action title 不暗示官方关系

## 2. 首次隐私同意

首次安装或从没有 consent 的旧版本升级后：

- [ ] 打开 ChatGPT 时首先显示一次性本地处理说明
- [ ] 文案明确说明：仅本机读取当前 ChatGPT 页面用于长会话趋势判断；不上传；不保存聊天正文；可卸载/清除扩展数据
- [ ] 未点击“同意并开始”前，不出现正常监测状态
- [ ] 点击“暂不开启”后面板收起并显示灰色“未启用”胶囊
- [ ] 刷新页面后不会再次自动弹出同意卡，只保留“未启用”胶囊
- [ ] 点击“未启用”胶囊可以重新打开同意卡
- [ ] 点击“同意并开始”后才开始正常监测
- [ ] 同意前 `chrome.storage.local` 中不存在聊天 ledger / message fingerprint 新增记录

## 3. 面板收敛

点击状态胶囊。

必须看到：

- [ ] 当前会话长度
- [ ] 本地风险趋势条
- [ ] 学习状态
- [ ] 复制续接提示词
- [ ] 重新学习
- [ ] 本会话暂不提醒
- [ ] 点击页面其他位置收起
- [ ] 按 Escape 收起
- [ ] 点击胶囊、面板和面板内按钮不因 click-away 关闭
- [ ] 窄屏下不出屏，面板位于胶囊上方且不遮挡输入框主要操作区

不得看到：

- [ ] 下一轮预测
- [ ] 统计完整性卡片
- [ ] 校准置信度百分比
- [ ] 确认安全至
- [ ] 历史风险区
- [ ] 太早 / 正好 / 太晚
- [ ] 5 轮后提醒
- [ ] 套餐/环境已变化
- [ ] 重新校准
- [ ] 恢复上一档案
- [ ] 清除全部学习数据
- [ ] token / tokens
- [ ] `≈` 后跟数字
- [ ] 数字 + K
- [ ] 阈值、百分比或可被理解为官方额度的数据
- [ ] 趋势条旁出现任何数值标签

“当前会话长度”只能显示正常范围、偏长、建议整理、接近风险区、暂时无法判断等模糊文案。

“本地风险趋势”只能显示视觉趋势，不得显示 token 数、百分比、K 值、阈值、总额度或剩余额度；无法可靠判断时应明显灰化。

底部免责声明必须为：

```text
仅作本地趋势判断，不代表 OpenAI 官方额度或上限。
```

## 4. 新会话 Coverage

从真正空白的新聊天页开始：

- [ ] 空白页无 conversation id 且无消息时进入 armed
- [ ] 发送第一条用户消息后继续观察
- [ ] 页面迁移到 `/c/<id>` 后，该会话可被标为从开始观察
- [ ] 刷新该会话后仍保持 complete

## 5. 旧会话 Coverage

- [ ] 从首页点击历史会话，不得继承 complete
- [ ] 直接打开旧 `/c/<id>`，显示旧会话历史可能不完整
- [ ] 只显示模糊长度判断，不显示已观察负载数字或确认安全结论
- [ ] 向上滚动加载更多历史时可补充匿名 ledger，重复加载不重复计数

## 6. 风险与提醒

- [ ] composer 草稿变化不改变风险依据
- [ ] assistant 预计增长不作为风险依据
- [ ] 风险随后台当前会话负载和已学习边界变化，但 UI 不显示具体数值
- [ ] parser unreliable 时显示无法可靠监测
- [ ] “本会话暂不提醒”只影响当前会话提醒
- [ ] “重新学习”创建新的学习代际，旧样本不与新代际直接平均

## 7. 续接提示词

点击“复制续接提示词”：

- [ ] 复制的是固定续接提示词
- [ ] 插件不会自动代用户发送
- [ ] 复制内容不包含当前聊天正文

## 8. 隐私检查

检查 `chrome.storage.local`：

允许看到：

- install salt
- 匿名 message fingerprint
- 内部 token / 字符估算
- branch / coverage / parser health
- generation / calibration
- reminder controls

不得看到：

- [ ] 用户聊天正文
- [ ] GPT 回复正文
- [ ] 输入框草稿正文
- [ ] 附件正文
- [ ] 邮箱 / 姓名
- [ ] OpenAI API Key

## 9. 发布阻断

出现以下任一情况不得发布：

1. 旧会话被错误标为完整
2. 面板仍出现已删除的旧功能入口
3. 正文进入持久化存储或日志
4. 非长度错误污染 Failure Ceiling
5. 解析失败时仍显示绿色安全状态
6. Chrome 或 Edge 任一无法加载
7. click-away / Escape 无法收起，或内部按钮点击被误关闭
8. 图标使用官方 logo、文字商标或暗示官方关系
