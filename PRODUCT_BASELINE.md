# 产品基线：ChatGPT 长会话提前预警器

## 1. 产品定位

一个独立的 Chrome / Edge 浏览器扩展，只服务于 `chatgpt.com` 网页端长对话。

它做的是本地估算和提前提醒，不读取 OpenAI 官方剩余额度，不声称知道精确会话上限。

## 2. V1 收敛范围

V1 只保留三件事：

- 当前会话长度的本地趋势判断
- 后台个体校准
- 临近已学习边界时提醒用户整理和续接

V1 用户可见界面只保留一个状态胶囊。点击胶囊后显示：

- 当前会话长度（正常范围 / 偏长 / 建议整理 / 接近风险区 / 暂时无法判断）
- 本地风险趋势条，不显示数字、比例或额度
- 学习状态
- 必要时提示旧会话历史可能不完整
- 复制续接提示词
- 重新学习
- 本会话暂不提醒

首次使用前必须显示一次性隐私同意卡。未获得“同意并开始”前：

- 不得读取 ChatGPT 页面正文
- 不得做 fingerprint 或 token 估算
- 不得注册会话观察 MutationObserver
- 不得注册发送监听
- 不得启动会话 ledger / 风险判断

用户点“暂不开启”后只显示灰色小胶囊“未启用”，点击胶囊可重新打开同意卡，不自动反复弹出。

面板行为：

- 右下角悬浮胶囊保持低打扰。
- 面板展开在胶囊上方、右侧对齐，受 viewport 约束。
- 点击页面其他位置或按 Escape 收起。
- 点击胶囊、面板或面板内按钮不收起。

## 3. V1 明确删除的用户可见内容

主面板不显示：

- 下一轮预测
- 统计完整性卡片
- 校准置信度百分比
- 确认安全至
- 历史风险区
- 太早 / 正好 / 太晚
- 5 轮后提醒
- 套餐/环境已变化
- 重新校准
- 恢复上一档案
- 清除全部学习数据

“套餐/环境已变化”和“重新校准”的用户入口合并为“重新学习”。

## 4. 风险原则

风险只基于：

- 当前会话负载
- 已学习的本地边界
- coverage / parser 的保守修正

V1 不再把 composer 草稿、预计 assistant 增长或下一轮预测作为风险依据。

用户界面不得显示任何具体 token 数、近似 token 数、K 数、阈值、百分比或可被理解为官方额度的数据。后台可以继续保存和计算 token/currentEstimatedLoad/校准边界，用于内部风险判断。

用户可见免责声明固定为：“仅作本地趋势判断，不代表 OpenAI 官方额度或上限。”

## 5. Coverage 原则

只有以下路径可以把会话标为从开始观察：

1. content script 确认当前处于真正空白新聊天页：无 conversation id 且无消息。
2. 在这个 armed 状态下观察到用户实际发送首条消息。
3. 之后迁移到 `/c/<id>` 的同一会话。

其他情况：

- 从首页点击历史会话不能继承 complete。
- 已持久化为 complete 的会话刷新后保持 complete。
- 直接打开旧 `/c/<id>` 仍为 incomplete，除非此前已经持久化 complete。

## 6. 隐私与边界

V1 默认：

- 无服务器
- 无账号系统
- 无 OpenAI API Key
- 不上传聊天正文
- 不持久化聊天正文、assistant 正文、composer 草稿正文、附件正文、姓名、邮箱、API Key
- 仅持久化匿名指纹、内部 token/字符估算、分支/coverage/parser 状态、校准结果和本地设置
- 页面内容只在用户同意后于本机处理，never transmitted to developer/server
- 用户可通过卸载扩展或清除扩展数据删除本地数据

V1 不做：

- Codex / CLI / API
- 其他 AI 网站
- 云同步
- 自动代用户发消息
- 自动创建新对话
- 以 OpenAI 未公开 backend API 作为核心路径

## 7. 品牌与图标

V1 使用用户选定并进一步简化的原创图标方案：深青绿圆角方形底板、白色聊天气泡、右下角高对比橙色守护盾牌与白色对勾。优先保证 16px / 32px 浏览器工具栏尺寸下的辨识度。

图标不得：

- 使用 ChatGPT / OpenAI logo 或六结标志
- 包含“ChatGPT”文字或字母品牌标记
- 暗示官方、授权或从属关系

公开品牌名、manifest name 与 `action.default_title` 统一使用“LongChat Guard”。`ChatGPT` 仅用于说明适用网页，不表示官方关系。
